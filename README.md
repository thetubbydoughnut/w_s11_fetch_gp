# Fetch Guided Project

## ✨ Requirements

1. Node >= 18.x
2. Git Bash (Windows users)

## ✨ Usage

1. `npm install`
2. `npm run dev`

## ✨ Endpoints

- `[GET] /api/books`
- `[DELETE] /api/books/:id`
- `[POST] /api/books`
- `[PUT] /api/books/:id`
